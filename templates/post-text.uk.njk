<img alt="Офіційний курс гривні на {{ dateLoc }} " src="{{ imgsrc }}">
<p>Національним Банком України (НБУ) {{ dateloc }} встановлено наступний курс гривні UAH до відповідних іноземних валют:</p>

{% for feature in features %}{% if feature.code=='UAH/USD' or feature.code=='UAH/EUR' or feature.code=='UAH/GBP'
or feature.code=='UAH/RUB' %}<p>{{ feature.code }} ({{ feature.currency }}) {{ feature.rate }}</p> {% endif %}{% endfor %}

<table class="table-finance">
<thead>
<tr>
<th>Код</th>
<th>одиниці</th>
<th>Курс</th>
<th class="mob_hidden">Дата</th>
<th class="mob_desksm_hidden">Назва</th>
</tr>
</thead>
<tbody>
{% for feature in features %}
<tr>
<td>{{ feature.code }}</td><td>{{ feature.amount|default('1')}}</td><td>{{ feature.rate }}</td>
<td class="mob_hidden">{{ dateloc2 }}</td><td class="mob_desksm_hidden">{{ feature.currency | upper }}</td>
</tr>
{% endfor %}
</tbody>
</table>
<p>* Курс валют НБУ, встановлений на останній робочий день тижня або в передсвятковий день, дійсний протягом наступних за ним вихідних або святкових днів, а також в перший робочий день.</p>
